1. Переписать разбор файла с данными, используя библиотеку `csv`.
2. Хранить данные об отдельном фильме не в словаре, а в объекте OpenStruct (библиотека `ostruct`). Переписать с использвоанием OpenStruct весь код из задания 3: это поможет немного укоротить код во многих местах, и сделать его более чистым.
3. сложнее Вывести статистику по месяцам — в каком сколько фильмов снято (вне зависимости от года), использовать библиотеку `date`.
